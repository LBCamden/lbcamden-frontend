<section role="banner" class="lbcamden-emergency lbcamden-emergency--{{ params.campaignClass }} {% if params.homepage %}lbcamden-emergency--homepage{% endif %} {{ params.classes }}">
  <div class="govuk-width-container">
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">
        <h2 class="lbcamden-emergency__heading {{ params.headingClasses }}">{{ params.heading }}</h2>

        {% if params.shortDescription %}
        <p class="lbcamden-emergency__description {{ params.descriptionClasses }}">
          {{ params.shortDescription }}
        </p>
        {% endif %}

        {% if params.link %}
        <a href="{{ params.link }}" class="lbcamden-emergency__link">
          {{ params.linkText }}
        </a>
        {% endif %}
      </div>
    </div>
  </div>
</section>
